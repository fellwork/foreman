const base = require.resolve('./configs/base')
const prettier = require.resolve('./configs/prettier')
const prettyBase = require.resolve('./configs/pretty-base')
const prettyType = require.resolve('./configs/pretty-type')
const shared = require.resolve('./shared')
const typescript = require.resolve('./configs/typescript')

export { base, prettier, prettyBase, prettyType, shared, typescript }
